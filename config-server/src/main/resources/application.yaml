spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/FacuOlivieri/Bruno-Negro.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
server:
  port: 6000